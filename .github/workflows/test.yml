name: ollama
on: push
jobs:
  ollama:
    runs-on: ubuntu-latest
    steps:
      - name: Test ollama action
        uses: jacobtomlinson/ollama-action@fix-8
        with:
          model: llama3.2:3b
          prompt: |
            How much wood would a woodchuck chuck if a woodchuck could chuck wood?
